using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ReturnState : State
{
    public ReturnState(Entity ent, FiniteStateMachine fsm) : base(ent, fsm)
    {
    }
}
